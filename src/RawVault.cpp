#include "RawVault.h"

// RawVault RawVault::parse(Bytes data) {
//     // TODO: Implement this
//     return RawVault(VaultHeader(std::array<uint8_t, 8>(), 0, std::array<uint8_t, 32>(), 0, 0),
//                     std::array<uint8_t, 32>(), std::array<uint8_t, 24>(), std::array<uint8_t,
//                     16>(), std::vector<uint8_t>());
// }
